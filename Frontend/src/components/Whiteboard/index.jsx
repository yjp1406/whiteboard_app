const Whiteboard = ({ tool, setTool, color, setColor }) => {
    return <canvas
        id="whiteboard"
        className="border border-dark h-100 w-100"
     
    ></canvas>;
}

export default Whiteboard;